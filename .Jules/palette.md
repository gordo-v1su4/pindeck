## 2024-05-24 - Accessibility Patterns in Form Actions
**Learning:** Icon-only buttons in complex forms (like uploaders) often get overlooked for accessibility labels, especially when they are "micro-interactions" like removing tags or clearing items.
**Action:** Always check `IconButton` and small utility `button` elements for `aria-label` or `title` attributes during review.
