---
description: Complete technology stack analysis for the Visuals application including versions and best practices
globs: **/*
alwaysApply: true
---
# Visuals Technology Stack

This rule outlines the complete technology stack for the **Visuals** image gallery application, including dependency versions, architecture decisions, and best practices recommendations.

## Application Overview

**Visuals** is a modern React/TypeScript image gallery built with:
- **Frontend**: React 19 + TypeScript + Vite + Tailwind CSS + Radix UI
- **Backend**: Convex (Backend-as-a-Service)
- **Build Tools**: Bun + ESLint + Prettier
- **Icons**: Radix UI Icons
- **Styling**: Tailwind CSS 4 with utility-first approach + Radix Themes
- **Tables**: TanStack Table for advanced data management

## Core Dependencies

### Runtime Dependencies

#### React Ecosystem
```json
"react": "^19.0.0",
"react-dom": "^19.0.0"
```
- **Version**: React 19.0.0 (Latest major release)
- **Best Practice**: ✅ **Excellent choice** - You're using the latest stable React version
- **Benefits**: New features, performance improvements, modern React patterns
- **Considerations**: Monitor for any breaking changes in minor updates

#### Backend & Database
```json
"convex": "1.31.2",
"@convex-dev/auth": "^0.0.90"
```
- **Version**: Convex 1.31.2, Auth 0.0.90
- **Best Practice**: ✅ **Modern BaaS choice** - Excellent for real-time applications
- **Benefits**: Real-time subscriptions, type-safe API, built-in authentication
- **Architecture**: Serverless backend with automatic scaling
- **Migration Note**: Updated to 1.31.2 with explicit table name syntax for `db.get()`, `db.patch()`, `db.delete()` operations

#### AI & Machine Learning
```json
"@google/generative-ai": "^0.24.1",
"@fal-ai/client": "^1.8.1"
```
- **Versions**: 
  - Google Generative AI 0.24.1 (latest available as of December 2024)
  - fal.ai Client 1.8.1 (latest available as of December 2024)
- **Image Analysis**: OpenRouter VLM Models (configurable, default: `qwen/qwen3-vl-8b-instruct`)
- **Image Generation Model**: fal.ai Nano Banana Pro (`fal-ai/nano-banana-pro/edit`)
- **Best Practice**: ✅ **Flexible and efficient vision models**
- **Features**: 
  - Image analysis (title, description, tags, colors, category, visual style) via OpenRouter VLM models
  - Supports any VLM model on OpenRouter (Gemini, Claude, GPT-4 Vision, etc.)
  - Provider routing for cost/performance optimization (price, throughput, latency)
  - Image generation with cinematic shot type variation via fal.ai Nano Banana Pro
  - Image-to-image editing for consistent subject and environment
- **Configuration**: 2K resolution, configurable aspect ratio, random shot type selection
- **API Access**: 
  - Requires `OPENROUTER_API_KEY` environment variable for OpenRouter API (image analysis)
  - Optional `OPENROUTER_VLM_MODEL` to specify VLM model (defaults to Gemini Flash 1.5)
  - Optional `OPENROUTER_PROVIDER_SORT` for provider routing strategy
  - Requires `FAL_KEY` environment variable for fal.ai API (image generation)

#### UI & Styling
```json
"@radix-ui/themes": "^3.2.1",
"@radix-ui/react-icons": "^1.3.2",
"tailwindcss": "^4",
"clsx": "^2.1.1",
"tailwind-merge": "^3.1.0"
```
- **Versions**: Radix Themes 3.2.1, Radix Icons 1.3.2, Tailwind 4, clsx 2.1.1, tailwind-merge 3.1.0
- **Best Practice**: ✅ **Modern UI component system**
- **Benefits**: Accessible components, consistent design system, utility-first CSS 4, class merging utilities
- **Pattern**: Radix UI for components + Tailwind 4 for styling + Radix Themes for theming

#### Utilities & Data Management
```json
"@tanstack/react-table": "^8.21.3",
"sonner": "^2.0.3",
"openai": "^6.15.0"
```
- **Versions**: TanStack Table 8.21.3, Sonner 2.0.3, OpenAI SDK 6.15.0
- **Best Practice**: ✅ **Modern data management stack**
- **Benefits**: Advanced table functionality (sorting, filtering, pagination), accessible toast notifications
- **Features**: Headless table components, real-time data updates, comprehensive filtering system
- **OpenAI SDK**: Used for OpenRouter API communication (compatible API)

## Development Dependencies

### Build Tools
```json
"vite": "^6.2.0",
"@vitejs/plugin-react": "^4.3.4"
```
- **Versions**: Vite 6.2.0, React Plugin 4.3.4
- **Best Practice**: ✅ **State-of-the-art build tool**
- **Benefits**: Lightning-fast HMR, optimized production builds
- **Performance**: Significantly faster than Webpack/CRA

### TypeScript
```json
"typescript": "~5.7.2",
"@types/react": "^19.0.10",
"@types/react-dom": "^19.0.4",
"@types/node": "^22.15.0"
```
- **Versions**: TS 5.7.2, React types 19.0.10, Node types 22.15.0
- **Best Practice**: ✅ **Excellent TypeScript setup**
- **Benefits**: Latest TS features, perfect React 19 compatibility
- **Configuration**: Strict typing with modern Node.js types

### Code Quality & Linting
```json
"eslint": "^9.21.0",
"@eslint/js": "^9.21.0",
"eslint-plugin-react-hooks": "^5.1.0",
"eslint-plugin-react-refresh": "^0.4.19",
"typescript-eslint": "^8.24.1"
```
- **Versions**: ESLint 9.21.0, React Hooks 5.1.0, TS-ESLint 8.24.1
- **Best Practice**: ✅ **Modern ESLint configuration**
- **Benefits**: Flat config format, React 19 support, TypeScript integration
- **Coverage**: Hooks rules, fast refresh compatibility

### CSS Processing
```json
"postcss": "~8",
"autoprefixer": "~10",
"@tailwindcss/postcss": "^4.1.18"
```
- **Versions**: PostCSS ~8, Autoprefixer ~10, Tailwind PostCSS 4.1.18
- **Best Practice**: ✅ **Modern CSS processing with Tailwind 4**
- **Benefits**: Modern CSS features, automatic vendor prefixing, Tailwind 4 engine integration

### Development Tools
```json
"concurrently": "^9.2.1",
"dotenv": "^16.4.7",
"prettier": "^3.5.3",
"@playwright/test": "^1.57.0",
"playwright": "^1.57.0",
"tw-animate-css": "^1.0.0"
```
- **Versions**: Concurrently 9.2.1, Dotenv 16.4.7, Prettier 3.5.3, Playwright 1.57.0, TW Animate 1.0.0
- **Best Practice**: ✅ **Essential dev tools**
- **Benefits**: Parallel process execution, environment management, consistent formatting, E2E testing with Playwright, Tailwind animations

## Package Manager

```json
// Using Bun (inferred from bun.lock)
"packageManager": "bun"
```
- **Best Practice**: ✅ **Modern package manager choice**
- **Benefits**: Faster installs, better performance than npm/yarn
- **Compatibility**: Drop-in replacement for npm scripts

## Architecture Analysis

### Frontend Architecture
```
React 19 + TypeScript + Vite + Radix UI
├── Component Structure: Functional components with hooks
├── State Management: Local state + Convex real-time queries
├── Styling: Radix Themes + Tailwind CSS with utility-first approach
├── UI Components: Radix UI primitives with custom styling
├── Data Tables: TanStack Table for advanced data management
├── Type Safety: Strict TypeScript configuration
└── Performance: Optimized with Vite's build system
```

### Backend Architecture
```
Convex (Backend-as-a-Service)
├── Database: Real-time document database
├── API: Type-safe generated functions
├── Auth: Built-in authentication system
├── File Storage: Integrated storage for images
├── Boards: Pinterest-style collections system
├── AI Vision: Gemini 3 Flash Preview integration
│   ├── Smart Analysis: Automatic metadata extraction
│   ├── Image Generation: Cinematic variations with random shot types
│   └── Memory Management: Optimized for 64MB limit
├── Color Extraction: Automatic color palette generation
└── Scaling: Automatic horizontal scaling
```

## Version Strategy & Best Practices

### ✅ Excellent Choices Made

1. **React 19.0.0**: Latest stable release with new features
2. **TypeScript 5.7.2**: Modern type system with latest features
3. **Vite 6.2.0**: Next-generation build tool
4. **Convex 1.31.2**: Modern BaaS with excellent DX (migrated to explicit table name API)
5. **Gemini 3 Flash Preview**: Fast and efficient vision AI for analysis and generation
6. **Bun 1.3.2**: Fast package manager
7. **Radix UI**: Accessible component primitives
8. **TanStack Table**: Advanced data table functionality
9. **Tailwind CSS 4**: Next-generation utility-first styling

### ✅ Recently Updated (December 2024)

```json
// Successfully updated to latest stable versions:
{
  "convex": "1.31.2",                   // ✅ Updated with DB API migration
  "tailwindcss": "^4",                  // ✅ Updated to Tailwind CSS 4
  "@types/node": "^22.15.0",            // ✅ Updated Node types
  "@google/generative-ai": "^0.24.1",  // ✅ Latest available Gemini SDK
  "vite": "^6.2.0"                      // ✅ Updated to Vite 6.2
}
```

### 📈 Future Updates to Consider

```json
// Monitor for future updates:
{
  "@radix-ui/themes": "^3.3.0",   // When available
  "@convex-dev/auth": "^0.0.91"   // When available
}
```

### 🔧 Maintenance Recommendations

1. **Version Pinning**: Use `~` for patch updates, `^` for minor updates
2. **Security**: Run `bun audit` regularly for vulnerability checks
3. **Updates**: Update dependencies quarterly for security patches
4. **Testing**: Test major version updates in staging environment
5. **Documentation**: Keep dependency decisions documented

### ⚠️ Potential Considerations

1. **React 19**: Monitor community adoption and any ecosystem updates
2. **Convex**: Evaluate usage costs as application scales
3. **Bun**: Ensure CI/CD pipeline supports Bun (or add npm fallback)
4. **TypeScript**: Leverage new TS 5.7 features (better error messages, performance)
5. **Convex DB API**: All database operations now use explicit table name syntax (`db.get("table", id)`) - migration completed December 2024

## Development Workflow

### Local Development
```bash
bun install          # Fast dependency installation
bun run dev         # Concurrent frontend + backend development
bun run lint        # TypeScript + ESLint checking
bun run build       # Production build with optimizations
```

### Code Quality Pipeline
```
Source Code → TypeScript → ESLint → Prettier → Vite Build
     ↓           ↓         ↓         ↓          ↓
   Strict      Modern    Consistent  Formatted  Optimized
  Typing      Rules     Code Style   Code      Bundle
```

## Performance Optimizations

### Build Performance
- **Vite**: Lightning-fast HMR and optimized production builds
- **Bun**: Faster package installation and script execution
- **ESLint 9**: Improved performance with flat config

### Runtime Performance
- **React 19**: Better performance with new architecture
- **Tailwind 4**: Minimal CSS bundle with new engine and purging
- **Convex**: Efficient real-time data synchronization
- **Radix Icons**: Tree-shakable icon imports

## Migration Paths

### If Needed: React Version Management
```typescript
// For gradual adoption of React 19 features
const isReact19 = React.version.startsWith('19');

// Feature flags for React 19 capabilities
const useNewFeatures = isReact19 && process.env.NODE_ENV === 'development';
```

### Package Manager Fallback
```json
// Alternative npm scripts for CI/CD compatibility
{
  "scripts": {
    "install:npm": "npm install",
    "dev:npm": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  }
}
```

## Summary

Your technology stack represents a **modern, high-performance, and developer-friendly** architecture that leverages the latest stable versions of key technologies. The choices made demonstrate excellent judgment in balancing cutting-edge features with stability and performance.

**Key Strengths:**
- ✅ Latest stable versions across the board
- ✅ Modern development experience
- ✅ Type-safe full-stack application
- ✅ Optimized build and runtime performance
- ✅ Excellent developer tooling

**Recommended Actions:**
1. Monitor React 19 ecosystem adoption
2. Keep dependencies updated quarterly
3. Consider adding automated dependency updates
4. Document any custom dependency decisions

This stack positions your project well for future growth and maintenance! 🚀