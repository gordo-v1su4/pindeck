# Convex Configuration
# Get these values from your Convex dashboard

# Required: Your Convex deployment URL
# Found in your Convex dashboard under "Settings" -> "General"
# Use .convex.cloud format (newer) - both CONVEX_URL and VITE_CONVEX_URL work
# If both are set, VITE_CONVEX_URL takes precedence
# Format: https://your-deployment-name.convex.cloud
CONVEX_URL=https://your-deployment-name.convex.cloud
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# Optional: Convex deployment identifier (set automatically by Convex CLI)
# CONVEX_DEPLOYMENT=dev:your-deployment-name

# Optional: Deploy Key for CI/CD deployments
# Found in your Convex dashboard under "Settings" -> "General"
# Only needed for automated deployments (not for local development)
# CONVEX_DEPLOY_KEY=dev:your-deployment-name|your-key-here

# Required: Your site URL for authentication
# For Vercel preview deployments, this will be your Vercel domain
# For local development, this can be http://localhost:3000
# Format: https://your-app-name.vercel.app
CONVEX_SITE_URL=https://your-app-name.vercel.app

# AI API Keys (set these in Convex Dashboard -> Settings -> Environment Variables)
# OPEN_ROUTER_KEY or OPENROUTER_API_KEY - Required for image analysis (Qwen3 VL 8B Instruct via OpenRouter)
#   The code accepts both OPEN_ROUTER_KEY and OPENROUTER_API_KEY
# OPENROUTER_VLM_MODEL - Optional: VLM model to use (default: "qwen/qwen3-vl-8b-instruct")
#   Examples: "qwen/qwen3-vl-8b-instruct" (default), "google/gemini-flash-1.5", 
#             "google/gemini-pro-vision", "anthropic/claude-3-opus", "openai/gpt-4-vision-preview"
# OPENROUTER_PROVIDER_SORT - Optional: Provider routing strategy ("price", "throughput", "latency")
# FAL_KEY - Required for image generation (Nano Banana Pro) - uses analysis results from OpenRouter

# Optional: Other API keys
# GOOGLE_API_KEY= (if needed for other services)
# N8N-MCP_API_KEY= (if using n8n MCP integration)
